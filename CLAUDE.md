# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project state

This repository is an early-stage scaffold for a library seat reservation application. It currently contains:

- `backend/` — Node.js/Express backend. Only `package.json` exists (Express ^5.2.1 as a dependency); no source files (e.g. `index.js`) have been written yet.
- `frontend/` — empty, not yet started.
- `README.md` — empty.

There is no build, lint, or test tooling configured yet (`backend/package.json`'s `test` script is the default placeholder that exits with an error). As the project grows, update this file with real commands (install, run, lint, test) and describe the actual architecture once it exists — don't assume structure that hasn't been built yet.
